{
  "prettier.enable": false,
  "editor.formatOnSave": false,

  // 에디터 저장시 eslint로 코드 자동 수정 설정
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit",
    // vscode 대신 eslint 에서 import 순서를 조정하도록 설정.
    "source.organizeImports": "never"
  }
}
